# PostgreSQL Configuration (V2/V3 AI-ready)
spring.datasource.url=${CRAWLERNEST_JDBC_URL:jdbc:postgresql://localhost:5432/clawer}
spring.datasource.username=${CRAWLERNEST_DB_USER:test}
spring.datasource.password=${CRAWLERNEST_DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=warehouse

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=200
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# JSONB support is native in Hibernate 6
