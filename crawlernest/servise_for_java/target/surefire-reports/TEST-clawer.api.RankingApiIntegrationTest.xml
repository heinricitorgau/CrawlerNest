<?xml version="1.0" encoding="UTF-8"?>
<testsuite xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://maven.apache.org/surefire/maven-surefire-plugin/xsd/surefire-test-report-3.0.xsd" version="3.0" name="clawer.api.RankingApiIntegrationTest" time="6.78" tests="18" errors="0" skipped="0" failures="0">
  <properties>
    <property name="java.specification.version" value="25"/>
    <property name="sun.jnu.encoding" value="UTF-8"/>
    <property name="java.class.path" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/test-classes:/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/classes:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-web/3.2.3/spring-boot-starter-web-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter/3.2.3/spring-boot-starter-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot/3.2.3/spring-boot-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-autoconfigure/3.2.3/spring-boot-autoconfigure-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-logging/3.2.3/spring-boot-starter-logging-3.2.3.jar:/Users/test/.m2/repository/ch/qos/logback/logback-classic/1.4.14/logback-classic-1.4.14.jar:/Users/test/.m2/repository/ch/qos/logback/logback-core/1.4.14/logback-core-1.4.14.jar:/Users/test/.m2/repository/org/apache/logging/log4j/log4j-to-slf4j/2.21.1/log4j-to-slf4j-2.21.1.jar:/Users/test/.m2/repository/org/apache/logging/log4j/log4j-api/2.21.1/log4j-api-2.21.1.jar:/Users/test/.m2/repository/org/slf4j/jul-to-slf4j/2.0.12/jul-to-slf4j-2.0.12.jar:/Users/test/.m2/repository/jakarta/annotation/jakarta.annotation-api/2.1.1/jakarta.annotation-api-2.1.1.jar:/Users/test/.m2/repository/org/yaml/snakeyaml/2.2/snakeyaml-2.2.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-json/3.2.3/spring-boot-starter-json-3.2.3.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-databind/2.15.4/jackson-databind-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-annotations/2.15.4/jackson-annotations-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-core/2.15.4/jackson-core-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/datatype/jackson-datatype-jdk8/2.15.4/jackson-datatype-jdk8-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/datatype/jackson-datatype-jsr310/2.15.4/jackson-datatype-jsr310-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/module/jackson-module-parameter-names/2.15.4/jackson-module-parameter-names-2.15.4.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-tomcat/3.2.3/spring-boot-starter-tomcat-3.2.3.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-core/10.1.19/tomcat-embed-core-10.1.19.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-el/10.1.19/tomcat-embed-el-10.1.19.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-websocket/10.1.19/tomcat-embed-websocket-10.1.19.jar:/Users/test/.m2/repository/org/springframework/spring-web/6.1.4/spring-web-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-beans/6.1.4/spring-beans-6.1.4.jar:/Users/test/.m2/repository/io/micrometer/micrometer-observation/1.12.3/micrometer-observation-1.12.3.jar:/Users/test/.m2/repository/io/micrometer/micrometer-commons/1.12.3/micrometer-commons-1.12.3.jar:/Users/test/.m2/repository/org/springframework/spring-webmvc/6.1.4/spring-webmvc-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-aop/6.1.4/spring-aop-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-context/6.1.4/spring-context-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-expression/6.1.4/spring-expression-6.1.4.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-data-jpa/3.2.3/spring-boot-starter-data-jpa-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-aop/3.2.3/spring-boot-starter-aop-3.2.3.jar:/Users/test/.m2/repository/org/aspectj/aspectjweaver/1.9.21/aspectjweaver-1.9.21.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-jdbc/3.2.3/spring-boot-starter-jdbc-3.2.3.jar:/Users/test/.m2/repository/com/zaxxer/HikariCP/5.0.1/HikariCP-5.0.1.jar:/Users/test/.m2/repository/org/springframework/spring-jdbc/6.1.4/spring-jdbc-6.1.4.jar:/Users/test/.m2/repository/org/hibernate/orm/hibernate-core/6.4.4.Final/hibernate-core-6.4.4.Final.jar:/Users/test/.m2/repository/jakarta/persistence/jakarta.persistence-api/3.1.0/jakarta.persistence-api-3.1.0.jar:/Users/test/.m2/repository/jakarta/transaction/jakarta.transaction-api/2.0.1/jakarta.transaction-api-2.0.1.jar:/Users/test/.m2/repository/org/jboss/logging/jboss-logging/3.5.3.Final/jboss-logging-3.5.3.Final.jar:/Users/test/.m2/repository/org/hibernate/common/hibernate-commons-annotations/6.0.6.Final/hibernate-commons-annotations-6.0.6.Final.jar:/Users/test/.m2/repository/io/smallrye/jandex/3.1.2/jandex-3.1.2.jar:/Users/test/.m2/repository/com/fasterxml/classmate/1.6.0/classmate-1.6.0.jar:/Users/test/.m2/repository/net/bytebuddy/byte-buddy/1.14.12/byte-buddy-1.14.12.jar:/Users/test/.m2/repository/org/glassfish/jaxb/jaxb-runtime/4.0.4/jaxb-runtime-4.0.4.jar:/Users/test/.m2/repository/org/glassfish/jaxb/jaxb-core/4.0.4/jaxb-core-4.0.4.jar:/Users/test/.m2/repository/org/eclipse/angus/angus-activation/2.0.1/angus-activation-2.0.1.jar:/Users/test/.m2/repository/org/glassfish/jaxb/txw2/4.0.4/txw2-4.0.4.jar:/Users/test/.m2/repository/com/sun/istack/istack-commons-runtime/4.1.2/istack-commons-runtime-4.1.2.jar:/Users/test/.m2/repository/jakarta/inject/jakarta.inject-api/2.0.1/jakarta.inject-api-2.0.1.jar:/Users/test/.m2/repository/org/antlr/antlr4-runtime/4.13.0/antlr4-runtime-4.13.0.jar:/Users/test/.m2/repository/org/springframework/data/spring-data-jpa/3.2.3/spring-data-jpa-3.2.3.jar:/Users/test/.m2/repository/org/springframework/data/spring-data-commons/3.2.3/spring-data-commons-3.2.3.jar:/Users/test/.m2/repository/org/springframework/spring-orm/6.1.4/spring-orm-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-tx/6.1.4/spring-tx-6.1.4.jar:/Users/test/.m2/repository/org/slf4j/slf4j-api/2.0.12/slf4j-api-2.0.12.jar:/Users/test/.m2/repository/org/springframework/spring-aspects/6.1.4/spring-aspects-6.1.4.jar:/Users/test/.m2/repository/org/postgresql/postgresql/42.6.1/postgresql-42.6.1.jar:/Users/test/.m2/repository/org/checkerframework/checker-qual/3.31.0/checker-qual-3.31.0.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-test/3.2.3/spring-boot-starter-test-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-test/3.2.3/spring-boot-test-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-test-autoconfigure/3.2.3/spring-boot-test-autoconfigure-3.2.3.jar:/Users/test/.m2/repository/com/jayway/jsonpath/json-path/2.9.0/json-path-2.9.0.jar:/Users/test/.m2/repository/jakarta/xml/bind/jakarta.xml.bind-api/4.0.1/jakarta.xml.bind-api-4.0.1.jar:/Users/test/.m2/repository/jakarta/activation/jakarta.activation-api/2.1.2/jakarta.activation-api-2.1.2.jar:/Users/test/.m2/repository/net/minidev/json-smart/2.5.0/json-smart-2.5.0.jar:/Users/test/.m2/repository/net/minidev/accessors-smart/2.5.0/accessors-smart-2.5.0.jar:/Users/test/.m2/repository/org/ow2/asm/asm/9.3/asm-9.3.jar:/Users/test/.m2/repository/org/assertj/assertj-core/3.24.2/assertj-core-3.24.2.jar:/Users/test/.m2/repository/org/awaitility/awaitility/4.2.0/awaitility-4.2.0.jar:/Users/test/.m2/repository/org/hamcrest/hamcrest/2.2/hamcrest-2.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter/5.10.2/junit-jupiter-5.10.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-api/5.10.2/junit-jupiter-api-5.10.2.jar:/Users/test/.m2/repository/org/opentest4j/opentest4j/1.3.0/opentest4j-1.3.0.jar:/Users/test/.m2/repository/org/junit/platform/junit-platform-commons/1.10.2/junit-platform-commons-1.10.2.jar:/Users/test/.m2/repository/org/apiguardian/apiguardian-api/1.1.2/apiguardian-api-1.1.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-params/5.10.2/junit-jupiter-params-5.10.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-engine/5.10.2/junit-jupiter-engine-5.10.2.jar:/Users/test/.m2/repository/org/junit/platform/junit-platform-engine/1.10.2/junit-platform-engine-1.10.2.jar:/Users/test/.m2/repository/org/mockito/mockito-core/5.7.0/mockito-core-5.7.0.jar:/Users/test/.m2/repository/net/bytebuddy/byte-buddy-agent/1.14.12/byte-buddy-agent-1.14.12.jar:/Users/test/.m2/repository/org/objenesis/objenesis/3.3/objenesis-3.3.jar:/Users/test/.m2/repository/org/mockito/mockito-junit-jupiter/5.7.0/mockito-junit-jupiter-5.7.0.jar:/Users/test/.m2/repository/org/skyscreamer/jsonassert/1.5.1/jsonassert-1.5.1.jar:/Users/test/.m2/repository/com/vaadin/external/google/android-json/0.0.20131108.vaadin1/android-json-0.0.20131108.vaadin1.jar:/Users/test/.m2/repository/org/springframework/spring-core/6.1.4/spring-core-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-jcl/6.1.4/spring-jcl-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-test/6.1.4/spring-test-6.1.4.jar:/Users/test/.m2/repository/org/xmlunit/xmlunit-core/2.9.1/xmlunit-core-2.9.1.jar:"/>
    <property name="java.vm.vendor" value="Oracle Corporation"/>
    <property name="sun.arch.data.model" value="64"/>
    <property name="net.bytebuddy.experimental" value="true"/>
    <property name="java.vendor.url" value="https://java.oracle.com/"/>
    <property name="user.timezone" value="Asia/Taipei"/>
    <property name="org.jboss.logging.provider" value="slf4j"/>
    <property name="os.name" value="Mac OS X"/>
    <property name="java.vm.specification.version" value="25"/>
    <property name="sun.java.launcher" value="SUN_STANDARD"/>
    <property name="user.country" value="TW"/>
    <property name="sun.boot.library.path" value="/Library/Java/JavaVirtualMachines/jdk-25.jdk/Contents/Home/lib"/>
    <property name="sun.java.command" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/surefire/surefirebooter-20260327170232512_3.jar /Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/surefire 2026-03-27T17-02-32_404-jvmRun1 surefire-20260327170232512_1tmp surefire_0-20260327170232512_2tmp"/>
    <property name="http.nonProxyHosts" value="local|*.local|169.254/16|*.169.254/16"/>
    <property name="jdk.debug" value="release"/>
    <property name="test" value="RankingApiIntegrationTest"/>
    <property name="surefire.test.class.path" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/test-classes:/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/classes:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-web/3.2.3/spring-boot-starter-web-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter/3.2.3/spring-boot-starter-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot/3.2.3/spring-boot-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-autoconfigure/3.2.3/spring-boot-autoconfigure-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-logging/3.2.3/spring-boot-starter-logging-3.2.3.jar:/Users/test/.m2/repository/ch/qos/logback/logback-classic/1.4.14/logback-classic-1.4.14.jar:/Users/test/.m2/repository/ch/qos/logback/logback-core/1.4.14/logback-core-1.4.14.jar:/Users/test/.m2/repository/org/apache/logging/log4j/log4j-to-slf4j/2.21.1/log4j-to-slf4j-2.21.1.jar:/Users/test/.m2/repository/org/apache/logging/log4j/log4j-api/2.21.1/log4j-api-2.21.1.jar:/Users/test/.m2/repository/org/slf4j/jul-to-slf4j/2.0.12/jul-to-slf4j-2.0.12.jar:/Users/test/.m2/repository/jakarta/annotation/jakarta.annotation-api/2.1.1/jakarta.annotation-api-2.1.1.jar:/Users/test/.m2/repository/org/yaml/snakeyaml/2.2/snakeyaml-2.2.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-json/3.2.3/spring-boot-starter-json-3.2.3.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-databind/2.15.4/jackson-databind-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-annotations/2.15.4/jackson-annotations-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/core/jackson-core/2.15.4/jackson-core-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/datatype/jackson-datatype-jdk8/2.15.4/jackson-datatype-jdk8-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/datatype/jackson-datatype-jsr310/2.15.4/jackson-datatype-jsr310-2.15.4.jar:/Users/test/.m2/repository/com/fasterxml/jackson/module/jackson-module-parameter-names/2.15.4/jackson-module-parameter-names-2.15.4.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-tomcat/3.2.3/spring-boot-starter-tomcat-3.2.3.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-core/10.1.19/tomcat-embed-core-10.1.19.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-el/10.1.19/tomcat-embed-el-10.1.19.jar:/Users/test/.m2/repository/org/apache/tomcat/embed/tomcat-embed-websocket/10.1.19/tomcat-embed-websocket-10.1.19.jar:/Users/test/.m2/repository/org/springframework/spring-web/6.1.4/spring-web-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-beans/6.1.4/spring-beans-6.1.4.jar:/Users/test/.m2/repository/io/micrometer/micrometer-observation/1.12.3/micrometer-observation-1.12.3.jar:/Users/test/.m2/repository/io/micrometer/micrometer-commons/1.12.3/micrometer-commons-1.12.3.jar:/Users/test/.m2/repository/org/springframework/spring-webmvc/6.1.4/spring-webmvc-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-aop/6.1.4/spring-aop-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-context/6.1.4/spring-context-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-expression/6.1.4/spring-expression-6.1.4.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-data-jpa/3.2.3/spring-boot-starter-data-jpa-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-aop/3.2.3/spring-boot-starter-aop-3.2.3.jar:/Users/test/.m2/repository/org/aspectj/aspectjweaver/1.9.21/aspectjweaver-1.9.21.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-jdbc/3.2.3/spring-boot-starter-jdbc-3.2.3.jar:/Users/test/.m2/repository/com/zaxxer/HikariCP/5.0.1/HikariCP-5.0.1.jar:/Users/test/.m2/repository/org/springframework/spring-jdbc/6.1.4/spring-jdbc-6.1.4.jar:/Users/test/.m2/repository/org/hibernate/orm/hibernate-core/6.4.4.Final/hibernate-core-6.4.4.Final.jar:/Users/test/.m2/repository/jakarta/persistence/jakarta.persistence-api/3.1.0/jakarta.persistence-api-3.1.0.jar:/Users/test/.m2/repository/jakarta/transaction/jakarta.transaction-api/2.0.1/jakarta.transaction-api-2.0.1.jar:/Users/test/.m2/repository/org/jboss/logging/jboss-logging/3.5.3.Final/jboss-logging-3.5.3.Final.jar:/Users/test/.m2/repository/org/hibernate/common/hibernate-commons-annotations/6.0.6.Final/hibernate-commons-annotations-6.0.6.Final.jar:/Users/test/.m2/repository/io/smallrye/jandex/3.1.2/jandex-3.1.2.jar:/Users/test/.m2/repository/com/fasterxml/classmate/1.6.0/classmate-1.6.0.jar:/Users/test/.m2/repository/net/bytebuddy/byte-buddy/1.14.12/byte-buddy-1.14.12.jar:/Users/test/.m2/repository/org/glassfish/jaxb/jaxb-runtime/4.0.4/jaxb-runtime-4.0.4.jar:/Users/test/.m2/repository/org/glassfish/jaxb/jaxb-core/4.0.4/jaxb-core-4.0.4.jar:/Users/test/.m2/repository/org/eclipse/angus/angus-activation/2.0.1/angus-activation-2.0.1.jar:/Users/test/.m2/repository/org/glassfish/jaxb/txw2/4.0.4/txw2-4.0.4.jar:/Users/test/.m2/repository/com/sun/istack/istack-commons-runtime/4.1.2/istack-commons-runtime-4.1.2.jar:/Users/test/.m2/repository/jakarta/inject/jakarta.inject-api/2.0.1/jakarta.inject-api-2.0.1.jar:/Users/test/.m2/repository/org/antlr/antlr4-runtime/4.13.0/antlr4-runtime-4.13.0.jar:/Users/test/.m2/repository/org/springframework/data/spring-data-jpa/3.2.3/spring-data-jpa-3.2.3.jar:/Users/test/.m2/repository/org/springframework/data/spring-data-commons/3.2.3/spring-data-commons-3.2.3.jar:/Users/test/.m2/repository/org/springframework/spring-orm/6.1.4/spring-orm-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-tx/6.1.4/spring-tx-6.1.4.jar:/Users/test/.m2/repository/org/slf4j/slf4j-api/2.0.12/slf4j-api-2.0.12.jar:/Users/test/.m2/repository/org/springframework/spring-aspects/6.1.4/spring-aspects-6.1.4.jar:/Users/test/.m2/repository/org/postgresql/postgresql/42.6.1/postgresql-42.6.1.jar:/Users/test/.m2/repository/org/checkerframework/checker-qual/3.31.0/checker-qual-3.31.0.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-starter-test/3.2.3/spring-boot-starter-test-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-test/3.2.3/spring-boot-test-3.2.3.jar:/Users/test/.m2/repository/org/springframework/boot/spring-boot-test-autoconfigure/3.2.3/spring-boot-test-autoconfigure-3.2.3.jar:/Users/test/.m2/repository/com/jayway/jsonpath/json-path/2.9.0/json-path-2.9.0.jar:/Users/test/.m2/repository/jakarta/xml/bind/jakarta.xml.bind-api/4.0.1/jakarta.xml.bind-api-4.0.1.jar:/Users/test/.m2/repository/jakarta/activation/jakarta.activation-api/2.1.2/jakarta.activation-api-2.1.2.jar:/Users/test/.m2/repository/net/minidev/json-smart/2.5.0/json-smart-2.5.0.jar:/Users/test/.m2/repository/net/minidev/accessors-smart/2.5.0/accessors-smart-2.5.0.jar:/Users/test/.m2/repository/org/ow2/asm/asm/9.3/asm-9.3.jar:/Users/test/.m2/repository/org/assertj/assertj-core/3.24.2/assertj-core-3.24.2.jar:/Users/test/.m2/repository/org/awaitility/awaitility/4.2.0/awaitility-4.2.0.jar:/Users/test/.m2/repository/org/hamcrest/hamcrest/2.2/hamcrest-2.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter/5.10.2/junit-jupiter-5.10.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-api/5.10.2/junit-jupiter-api-5.10.2.jar:/Users/test/.m2/repository/org/opentest4j/opentest4j/1.3.0/opentest4j-1.3.0.jar:/Users/test/.m2/repository/org/junit/platform/junit-platform-commons/1.10.2/junit-platform-commons-1.10.2.jar:/Users/test/.m2/repository/org/apiguardian/apiguardian-api/1.1.2/apiguardian-api-1.1.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-params/5.10.2/junit-jupiter-params-5.10.2.jar:/Users/test/.m2/repository/org/junit/jupiter/junit-jupiter-engine/5.10.2/junit-jupiter-engine-5.10.2.jar:/Users/test/.m2/repository/org/junit/platform/junit-platform-engine/1.10.2/junit-platform-engine-1.10.2.jar:/Users/test/.m2/repository/org/mockito/mockito-core/5.7.0/mockito-core-5.7.0.jar:/Users/test/.m2/repository/net/bytebuddy/byte-buddy-agent/1.14.12/byte-buddy-agent-1.14.12.jar:/Users/test/.m2/repository/org/objenesis/objenesis/3.3/objenesis-3.3.jar:/Users/test/.m2/repository/org/mockito/mockito-junit-jupiter/5.7.0/mockito-junit-jupiter-5.7.0.jar:/Users/test/.m2/repository/org/skyscreamer/jsonassert/1.5.1/jsonassert-1.5.1.jar:/Users/test/.m2/repository/com/vaadin/external/google/android-json/0.0.20131108.vaadin1/android-json-0.0.20131108.vaadin1.jar:/Users/test/.m2/repository/org/springframework/spring-core/6.1.4/spring-core-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-jcl/6.1.4/spring-jcl-6.1.4.jar:/Users/test/.m2/repository/org/springframework/spring-test/6.1.4/spring-test-6.1.4.jar:/Users/test/.m2/repository/org/xmlunit/xmlunit-core/2.9.1/xmlunit-core-2.9.1.jar:"/>
    <property name="sun.cpu.endian" value="little"/>
    <property name="user.home" value="/Users/test"/>
    <property name="user.language" value="zh"/>
    <property name="java.specification.vendor" value="Oracle Corporation"/>
    <property name="java.version.date" value="2025-10-21"/>
    <property name="java.home" value="/Library/Java/JavaVirtualMachines/jdk-25.jdk/Contents/Home"/>
    <property name="file.separator" value="/"/>
    <property name="basedir" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java"/>
    <property name="java.vm.compressedOopsMode" value="Zero based"/>
    <property name="line.separator" value="&#10;"/>
    <property name="java.vm.specification.vendor" value="Oracle Corporation"/>
    <property name="java.specification.name" value="Java Platform API Specification"/>
    <property name="FILE_LOG_CHARSET" value="UTF-8"/>
    <property name="java.awt.headless" value="true"/>
    <property name="apple.awt.application.name" value="ForkedBooter"/>
    <property name="surefire.real.class.path" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java/target/surefire/surefirebooter-20260327170232512_3.jar"/>
    <property name="user.script" value="Hant"/>
    <property name="sun.management.compiler" value="HotSpot 64-Bit Tiered Compilers"/>
    <property name="ftp.nonProxyHosts" value="local|*.local|169.254/16|*.169.254/16"/>
    <property name="java.runtime.version" value="25.0.1+8-LTS-27"/>
    <property name="user.name" value="test"/>
    <property name="stdout.encoding" value="UTF-8"/>
    <property name="path.separator" value=":"/>
    <property name="os.version" value="26.4"/>
    <property name="java.runtime.name" value="Java(TM) SE Runtime Environment"/>
    <property name="file.encoding" value="UTF-8"/>
    <property name="java.vm.name" value="Java HotSpot(TM) 64-Bit Server VM"/>
    <property name="localRepository" value="/Users/test/.m2/repository"/>
    <property name="java.vendor.url.bug" value="https://bugreport.java.com/bugreport/"/>
    <property name="java.io.tmpdir" value="/var/folders/yc/0h52s31s015bfm9gl_qh25vw0000gn/T/"/>
    <property name="com.zaxxer.hikari.pool_number" value="1"/>
    <property name="java.version" value="25.0.1"/>
    <property name="user.dir" value="/Users/test/Desktop/crawlernest/crawlernest/servise_for_java"/>
    <property name="os.arch" value="aarch64"/>
    <property name="java.vm.specification.name" value="Java Virtual Machine Specification"/>
    <property name="PID" value="64007"/>
    <property name="CONSOLE_LOG_CHARSET" value="UTF-8"/>
    <property name="native.encoding" value="UTF-8"/>
    <property name="java.library.path" value="/Users/test/Library/Java/Extensions:/Library/Java/Extensions:/Network/Library/Java/Extensions:/System/Library/Java/Extensions:/usr/lib/java:."/>
    <property name="java.vm.info" value="mixed mode, sharing"/>
    <property name="stderr.encoding" value="UTF-8"/>
    <property name="java.vendor" value="Oracle Corporation"/>
    <property name="java.vm.version" value="25.0.1+8-LTS-27"/>
    <property name="stdin.encoding" value="UTF-8"/>
    <property name="sun.io.unicode.encoding" value="UnicodeBig"/>
    <property name="socksNonProxyHosts" value="local|*.local|169.254/16|*.169.254/16"/>
    <property name="java.class.version" value="69.0"/>
  </properties>
  <testcase name="regionAggregationIncludesCoverageBeyondGlobalTopSlice" classname="clawer.api.RankingApiIntegrationTest" time="0.818">
    <system-out><![CDATA[17:02:33.396 [main] INFO org.springframework.test.context.support.AnnotationConfigContextLoaderUtils -- Could not detect default configuration classes for test class [clawer.api.RankingApiIntegrationTest]: RankingApiIntegrationTest does not declare any static, non-private, non-final, nested classes annotated with @Configuration.
17:02:33.579 [main] INFO org.springframework.boot.test.context.SpringBootTestContextBootstrapper -- Found @SpringBootConfiguration clawer.Application for test class clawer.api.RankingApiIntegrationTest

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.2.3)

2026-03-27T17:02:33.991+08:00  INFO 64007 --- [           main] clawer.api.RankingApiIntegrationTest     : Starting RankingApiIntegrationTest using Java 25.0.1 with PID 64007 (started by test in /Users/test/Desktop/crawlernest/crawlernest/servise_for_java)
2026-03-27T17:02:33.993+08:00  INFO 64007 --- [           main] clawer.api.RankingApiIntegrationTest     : No active profile set, falling back to 1 default profile: "default"
2026-03-27T17:02:33.994+08:00 DEBUG 64007 --- [           main] o.s.boot.SpringApplication               : Loading source class clawer.Application
2026-03-27T17:02:34.014+08:00 DEBUG 64007 --- [           main] o.s.w.c.s.GenericWebApplicationContext   : Refreshing org.springframework.web.context.support.GenericWebApplicationContext@5aceec94
2026-03-27T17:02:34.729+08:00  INFO 64007 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data JPA repositories in DEFAULT mode.
2026-03-27T17:02:34.747+08:00 DEBUG 64007 --- [           main] o.s.b.a.AutoConfigurationPackages        : @EnableAutoConfiguration was declared on a class in the package 'clawer'. Automatic @Repository and @Entity scanning is enabled.
2026-03-27T17:02:34.798+08:00  INFO 64007 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 61 ms. Found 2 JPA repository interfaces.
2026-03-27T17:02:35.398+08:00  INFO 64007 --- [           main] o.hibernate.jpa.internal.util.LogHelper  : HHH000204: Processing PersistenceUnitInfo [name: default]
2026-03-27T17:02:35.460+08:00  INFO 64007 --- [           main] org.hibernate.Version                    : HHH000412: Hibernate ORM core version 6.4.4.Final
2026-03-27T17:02:35.526+08:00  INFO 64007 --- [           main] o.h.c.internal.RegionFactoryInitiator    : HHH000026: Second-level cache disabled
2026-03-27T17:02:35.774+08:00  INFO 64007 --- [           main] o.s.o.j.p.SpringPersistenceUnitInfo      : No LoadTimeWeaver setup: ignoring JPA class transformer
2026-03-27T17:02:35.797+08:00  INFO 64007 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Starting...
2026-03-27T17:02:35.956+08:00  INFO 64007 --- [           main] com.zaxxer.hikari.pool.HikariPool        : HikariPool-1 - Added connection org.postgresql.jdbc.PgConnection@1d99ee1b
2026-03-27T17:02:35.957+08:00  INFO 64007 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Start completed.
2026-03-27T17:02:35.993+08:00  WARN 64007 --- [           main] org.hibernate.orm.deprecation            : HHH90000025: PostgreSQLDialect does not need to be specified explicitly using 'hibernate.dialect' (remove the property setting and it will be selected by default)
2026-03-27T17:02:37.011+08:00  INFO 64007 --- [           main] o.h.e.t.j.p.i.JtaPlatformInitiator       : HHH000489: No JTA platform available (set 'hibernate.transaction.jta.platform' to enable JTA platform integration)
2026-03-27T17:02:37.014+08:00  INFO 64007 --- [           main] j.LocalContainerEntityManagerFactoryBean : Initialized JPA EntityManagerFactory for persistence unit 'default'
2026-03-27T17:02:37.759+08:00  WARN 64007 --- [           main] JpaBaseConfiguration$JpaWebConfiguration : spring.jpa.open-in-view is enabled by default. Therefore, database queries may be performed during view rendering. Explicitly configure spring.jpa.open-in-view to disable this warning
2026-03-27T17:02:37.851+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : 10 mappings in 'requestMappingHandlerMapping'
2026-03-27T17:02:37.933+08:00 DEBUG 64007 --- [           main] o.s.w.s.handler.SimpleUrlHandlerMapping  : Patterns [/webjars/**, /**] in 'resourceHandlerMapping'
2026-03-27T17:02:37.968+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerAdapter : ControllerAdvice beans: 0 @ModelAttribute, 0 @InitBinder, 1 RequestBodyAdvice, 1 ResponseBodyAdvice
2026-03-27T17:02:38.001+08:00 DEBUG 64007 --- [           main] .m.m.a.ExceptionHandlerExceptionResolver : ControllerAdvice beans: 0 @ExceptionHandler, 1 ResponseBodyAdvice
2026-03-27T17:02:38.095+08:00 DEBUG 64007 --- [           main] o.s.b.w.s.ServletContextInitializerBeans : Mapping filters: characterEncodingFilter urls=[/*] order=-2147483648, formContentFilter urls=[/*] order=-9900, requestContextFilter urls=[/*] order=-105
2026-03-27T17:02:38.095+08:00 DEBUG 64007 --- [           main] o.s.b.w.s.ServletContextInitializerBeans : Mapping servlets: 
2026-03-27T17:02:38.105+08:00 DEBUG 64007 --- [           main] s.b.w.s.f.OrderedCharacterEncodingFilter : Filter 'characterEncodingFilter' configured for use
2026-03-27T17:02:38.106+08:00 DEBUG 64007 --- [           main] o.s.b.w.s.f.OrderedFormContentFilter     : Filter 'formContentFilter' configured for use
2026-03-27T17:02:38.106+08:00 DEBUG 64007 --- [           main] o.s.b.w.s.f.OrderedRequestContextFilter  : Filter 'requestContextFilter' configured for use
2026-03-27T17:02:38.107+08:00  INFO 64007 --- [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring TestDispatcherServlet ''
2026-03-27T17:02:38.107+08:00  INFO 64007 --- [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet ''
2026-03-27T17:02:38.109+08:00  INFO 64007 --- [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initialization in 2 ms
2026-03-27T17:02:38.141+08:00 DEBUG 64007 --- [           main] .s.b.a.l.ConditionEvaluationReportLogger : 


============================
CONDITIONS EVALUATION REPORT
============================


Positive matches:
-----------------

   AopAutoConfiguration matched:
      - @ConditionalOnProperty (spring.aop.auto=true) matched (OnPropertyCondition)

   AopAutoConfiguration.AspectJAutoProxyingConfiguration matched:
      - @ConditionalOnClass found required class 'org.aspectj.weaver.Advice' (OnClassCondition)

   AopAutoConfiguration.AspectJAutoProxyingConfiguration.CglibAutoProxyConfiguration matched:
      - @ConditionalOnProperty (spring.aop.proxy-target-class=true) matched (OnPropertyCondition)

   ApplicationAvailabilityAutoConfiguration#applicationAvailability matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.availability.ApplicationAvailability; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourceAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'javax.sql.DataSource', 'org.springframework.jdbc.datasource.embedded.EmbeddedDatabaseType' (OnClassCondition)
      - @ConditionalOnMissingBean (types: io.r2dbc.spi.ConnectionFactory; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourceAutoConfiguration.PooledDataSourceConfiguration matched:
      - AnyNestedCondition 1 matched 1 did not; NestedCondition on DataSourceAutoConfiguration.PooledDataSourceCondition.PooledDataSourceAvailable PooledDataSource found supported DataSource; NestedCondition on DataSourceAutoConfiguration.PooledDataSourceCondition.ExplicitType @ConditionalOnProperty (spring.datasource.type) did not find property 'type' (DataSourceAutoConfiguration.PooledDataSourceCondition)
      - @ConditionalOnMissingBean (types: javax.sql.DataSource,javax.sql.XADataSource; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourceAutoConfiguration.PooledDataSourceConfiguration#jdbcConnectionDetails matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.jdbc.JdbcConnectionDetails; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourceConfiguration.Hikari matched:
      - @ConditionalOnClass found required class 'com.zaxxer.hikari.HikariDataSource' (OnClassCondition)
      - @ConditionalOnProperty (spring.datasource.type=com.zaxxer.hikari.HikariDataSource) matched (OnPropertyCondition)
      - @ConditionalOnMissingBean (types: javax.sql.DataSource; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourceInitializationConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.jdbc.datasource.init.DatabasePopulator' (OnClassCondition)
      - @ConditionalOnSingleCandidate (types: javax.sql.DataSource; SearchStrategy: all) found a single bean 'dataSource'; @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.sql.init.SqlDataSourceScriptDatabaseInitializer,org.springframework.boot.autoconfigure.sql.init.SqlR2dbcScriptDatabaseInitializer; SearchStrategy: all) did not find any beans (OnBeanCondition)

   DataSourcePoolMetadataProvidersConfiguration.HikariPoolDataSourceMetadataProviderConfiguration matched:
      - @ConditionalOnClass found required class 'com.zaxxer.hikari.HikariDataSource' (OnClassCondition)

   DataSourceTransactionManagerAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'org.springframework.jdbc.core.JdbcTemplate', 'org.springframework.transaction.TransactionManager' (OnClassCondition)

   DataSourceTransactionManagerAutoConfiguration.JdbcTransactionManagerConfiguration matched:
      - @ConditionalOnSingleCandidate (types: javax.sql.DataSource; SearchStrategy: all) found a single bean 'dataSource' (OnBeanCondition)

   DispatcherServletAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.web.servlet.DispatcherServlet' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)

   DispatcherServletAutoConfiguration.DispatcherServletConfiguration matched:
      - @ConditionalOnClass found required class 'jakarta.servlet.ServletRegistration' (OnClassCondition)
      - Default DispatcherServlet did not find dispatcher servlet beans (DispatcherServletAutoConfiguration.DefaultDispatcherServletCondition)

   DispatcherServletAutoConfiguration.DispatcherServletRegistrationConfiguration matched:
      - @ConditionalOnClass found required class 'jakarta.servlet.ServletRegistration' (OnClassCondition)
      - DispatcherServlet Registration did not find servlet registration bean (DispatcherServletAutoConfiguration.DispatcherServletRegistrationCondition)

   DispatcherServletAutoConfiguration.DispatcherServletRegistrationConfiguration#dispatcherServletRegistration matched:
      - @ConditionalOnBean (names: dispatcherServlet types: org.springframework.web.servlet.DispatcherServlet; SearchStrategy: all) found bean 'dispatcherServlet' (OnBeanCondition)

   ErrorMvcAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'jakarta.servlet.Servlet', 'org.springframework.web.servlet.DispatcherServlet' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)

   ErrorMvcAutoConfiguration#basicErrorController matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.web.servlet.error.ErrorController; SearchStrategy: current) did not find any beans (OnBeanCondition)

   ErrorMvcAutoConfiguration#errorAttributes matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.web.servlet.error.ErrorAttributes; SearchStrategy: current) did not find any beans (OnBeanCondition)

   ErrorMvcAutoConfiguration.DefaultErrorViewResolverConfiguration#conventionErrorViewResolver matched:
      - @ConditionalOnBean (types: org.springframework.web.servlet.DispatcherServlet; SearchStrategy: all) found bean 'dispatcherServlet'; @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.web.servlet.error.ErrorViewResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   ErrorMvcAutoConfiguration.WhitelabelErrorViewConfiguration matched:
      - @ConditionalOnProperty (server.error.whitelabel.enabled) matched (OnPropertyCondition)
      - ErrorTemplate Missing did not find error template view (ErrorMvcAutoConfiguration.ErrorTemplateMissingCondition)

   ErrorMvcAutoConfiguration.WhitelabelErrorViewConfiguration#beanNameViewResolver matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.servlet.view.BeanNameViewResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   ErrorMvcAutoConfiguration.WhitelabelErrorViewConfiguration#defaultErrorView matched:
      - @ConditionalOnMissingBean (names: error; SearchStrategy: all) did not find any beans (OnBeanCondition)

   GenericCacheConfiguration matched:
      - Cache org.springframework.boot.autoconfigure.cache.GenericCacheConfiguration automatic cache type (CacheCondition)

   HibernateJpaAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean', 'jakarta.persistence.EntityManager', 'org.hibernate.engine.spi.SessionImplementor' (OnClassCondition)

   HibernateJpaConfiguration matched:
      - @ConditionalOnSingleCandidate (types: javax.sql.DataSource; SearchStrategy: all) found a single bean 'dataSource' (OnBeanCondition)

   HttpEncodingAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.web.filter.CharacterEncodingFilter' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)
      - @ConditionalOnProperty (server.servlet.encoding.enabled) matched (OnPropertyCondition)

   HttpEncodingAutoConfiguration#characterEncodingFilter matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.filter.CharacterEncodingFilter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   HttpMessageConvertersAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.http.converter.HttpMessageConverter' (OnClassCondition)
      - NoneNestedConditions 0 matched 1 did not; NestedCondition on HttpMessageConvertersAutoConfiguration.NotReactiveWebApplicationCondition.ReactiveWebApplication did not find reactive web application classes (HttpMessageConvertersAutoConfiguration.NotReactiveWebApplicationCondition)

   HttpMessageConvertersAutoConfiguration#messageConverters matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.http.HttpMessageConverters; SearchStrategy: all) did not find any beans (OnBeanCondition)

   HttpMessageConvertersAutoConfiguration.StringHttpMessageConverterConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.http.converter.StringHttpMessageConverter' (OnClassCondition)

   HttpMessageConvertersAutoConfiguration.StringHttpMessageConverterConfiguration#stringHttpMessageConverter matched:
      - @ConditionalOnMissingBean (types: org.springframework.http.converter.StringHttpMessageConverter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JacksonAutoConfiguration matched:
      - @ConditionalOnClass found required class 'com.fasterxml.jackson.databind.ObjectMapper' (OnClassCondition)

   JacksonAutoConfiguration.Jackson2ObjectMapperBuilderCustomizerConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.http.converter.json.Jackson2ObjectMapperBuilder' (OnClassCondition)

   JacksonAutoConfiguration.JacksonObjectMapperBuilderConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.http.converter.json.Jackson2ObjectMapperBuilder' (OnClassCondition)

   JacksonAutoConfiguration.JacksonObjectMapperBuilderConfiguration#jacksonObjectMapperBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.http.converter.json.Jackson2ObjectMapperBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JacksonAutoConfiguration.JacksonObjectMapperConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.http.converter.json.Jackson2ObjectMapperBuilder' (OnClassCondition)

   JacksonAutoConfiguration.JacksonObjectMapperConfiguration#jacksonObjectMapper matched:
      - @ConditionalOnMissingBean (types: com.fasterxml.jackson.databind.ObjectMapper; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JacksonAutoConfiguration.ParameterNamesModuleConfiguration matched:
      - @ConditionalOnClass found required class 'com.fasterxml.jackson.module.paramnames.ParameterNamesModule' (OnClassCondition)

   JacksonAutoConfiguration.ParameterNamesModuleConfiguration#parameterNamesModule matched:
      - @ConditionalOnMissingBean (types: com.fasterxml.jackson.module.paramnames.ParameterNamesModule; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JacksonHttpMessageConvertersConfiguration.MappingJackson2HttpMessageConverterConfiguration matched:
      - @ConditionalOnClass found required class 'com.fasterxml.jackson.databind.ObjectMapper' (OnClassCondition)
      - @ConditionalOnProperty (spring.mvc.converters.preferred-json-mapper=jackson) matched (OnPropertyCondition)
      - @ConditionalOnBean (types: com.fasterxml.jackson.databind.ObjectMapper; SearchStrategy: all) found bean 'jacksonObjectMapper' (OnBeanCondition)

   JacksonHttpMessageConvertersConfiguration.MappingJackson2HttpMessageConverterConfiguration#mappingJackson2HttpMessageConverter matched:
      - @ConditionalOnMissingBean (types: org.springframework.http.converter.json.MappingJackson2HttpMessageConverter ignored: org.springframework.hateoas.server.mvc.TypeConstrainedMappingJackson2HttpMessageConverter,org.springframework.data.rest.webmvc.alps.AlpsJsonHttpMessageConverter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JdbcClientAutoConfiguration matched:
      - @ConditionalOnSingleCandidate (types: org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate; SearchStrategy: all) found a single bean 'namedParameterJdbcTemplate'; @ConditionalOnMissingBean (types: org.springframework.jdbc.core.simple.JdbcClient; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JdbcTemplateAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'javax.sql.DataSource', 'org.springframework.jdbc.core.JdbcTemplate' (OnClassCondition)
      - @ConditionalOnSingleCandidate (types: javax.sql.DataSource; SearchStrategy: all) found a single bean 'dataSource' (OnBeanCondition)

   JdbcTemplateConfiguration matched:
      - @ConditionalOnMissingBean (types: org.springframework.jdbc.core.JdbcOperations; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration#entityManagerFactory matched:
      - @ConditionalOnMissingBean (types: org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean,jakarta.persistence.EntityManagerFactory; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration#entityManagerFactoryBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.orm.jpa.EntityManagerFactoryBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration#jpaVendorAdapter matched:
      - @ConditionalOnMissingBean (types: org.springframework.orm.jpa.JpaVendorAdapter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration#transactionManager matched:
      - @ConditionalOnMissingBean (types: org.springframework.transaction.TransactionManager; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration.JpaWebConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.web.servlet.config.annotation.WebMvcConfigurer' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)
      - @ConditionalOnProperty (spring.jpa.open-in-view=true) matched (OnPropertyCondition)
      - @ConditionalOnMissingBean (types: org.springframework.orm.jpa.support.OpenEntityManagerInViewInterceptor,org.springframework.orm.jpa.support.OpenEntityManagerInViewFilter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration.PersistenceManagedTypesConfiguration matched:
      - @ConditionalOnMissingBean (types: org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean,jakarta.persistence.EntityManagerFactory; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaBaseConfiguration.PersistenceManagedTypesConfiguration#persistenceManagedTypes matched:
      - @ConditionalOnMissingBean (types: org.springframework.orm.jpa.persistenceunit.PersistenceManagedTypes; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JpaRepositoriesAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.data.jpa.repository.JpaRepository' (OnClassCondition)
      - @ConditionalOnProperty (spring.data.jpa.repositories.enabled=true) matched (OnPropertyCondition)
      - @ConditionalOnBean (types: javax.sql.DataSource; SearchStrategy: all) found bean 'dataSource'; @ConditionalOnMissingBean (types: org.springframework.data.jpa.repository.support.JpaRepositoryFactoryBean,org.springframework.data.jpa.repository.config.JpaRepositoryConfigExtension; SearchStrategy: all) did not find any beans (OnBeanCondition)

   JtaAutoConfiguration matched:
      - @ConditionalOnClass found required class 'jakarta.transaction.Transaction' (OnClassCondition)
      - @ConditionalOnProperty (spring.jta.enabled) matched (OnPropertyCondition)

   LifecycleAutoConfiguration#defaultLifecycleProcessor matched:
      - @ConditionalOnMissingBean (names: lifecycleProcessor; SearchStrategy: current) did not find any beans (OnBeanCondition)

   MockMvcAutoConfiguration matched:
      - found 'session' scope (OnWebApplicationCondition)

   MockMvcAutoConfiguration#mockMvc matched:
      - @ConditionalOnMissingBean (types: org.springframework.test.web.servlet.MockMvc; SearchStrategy: all) did not find any beans (OnBeanCondition)

   MockMvcAutoConfiguration#mockMvcBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.test.web.servlet.MockMvcBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   MultipartAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'jakarta.servlet.Servlet', 'org.springframework.web.multipart.support.StandardServletMultipartResolver', 'jakarta.servlet.MultipartConfigElement' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)
      - @ConditionalOnProperty (spring.servlet.multipart.enabled) matched (OnPropertyCondition)

   MultipartAutoConfiguration#multipartConfigElement matched:
      - @ConditionalOnMissingBean (types: jakarta.servlet.MultipartConfigElement; SearchStrategy: all) did not find any beans (OnBeanCondition)

   MultipartAutoConfiguration#multipartResolver matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.multipart.MultipartResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   NamedParameterJdbcTemplateConfiguration matched:
      - @ConditionalOnSingleCandidate (types: org.springframework.jdbc.core.JdbcTemplate; SearchStrategy: all) found a single bean 'jdbcTemplate'; @ConditionalOnMissingBean (types: org.springframework.jdbc.core.namedparam.NamedParameterJdbcOperations; SearchStrategy: all) did not find any beans (OnBeanCondition)

   NoOpCacheConfiguration matched:
      - Cache org.springframework.boot.autoconfigure.cache.NoOpCacheConfiguration automatic cache type (CacheCondition)

   PersistenceExceptionTranslationAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor' (OnClassCondition)

   PersistenceExceptionTranslationAutoConfiguration#persistenceExceptionTranslationPostProcessor matched:
      - @ConditionalOnProperty (spring.dao.exceptiontranslation.enabled) matched (OnPropertyCondition)
      - @ConditionalOnMissingBean (types: org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor; SearchStrategy: all) did not find any beans (OnBeanCondition)

   PropertyPlaceholderAutoConfiguration#propertySourcesPlaceholderConfigurer matched:
      - @ConditionalOnMissingBean (types: org.springframework.context.support.PropertySourcesPlaceholderConfigurer; SearchStrategy: current) did not find any beans (OnBeanCondition)

   RestClientAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.web.client.RestClient' (OnClassCondition)
      - NoneNestedConditions 0 matched 1 did not; NestedCondition on NotReactiveWebApplicationCondition.ReactiveWebApplication did not find reactive web application classes (NotReactiveWebApplicationCondition)

   RestClientAutoConfiguration#httpMessageConvertersRestClientCustomizer matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.web.client.HttpMessageConvertersRestClientCustomizer; SearchStrategy: all) did not find any beans (OnBeanCondition)

   RestClientAutoConfiguration#restClientBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.client.RestClient$Builder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   RestClientAutoConfiguration#restClientBuilderConfigurer matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.web.client.RestClientBuilderConfigurer; SearchStrategy: all) did not find any beans (OnBeanCondition)

   RestClientAutoConfiguration#restClientSsl matched:
      - @ConditionalOnBean (types: org.springframework.boot.ssl.SslBundles; SearchStrategy: all) found bean 'sslBundleRegistry'; @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.web.client.RestClientSsl; SearchStrategy: all) did not find any beans (OnBeanCondition)

   RestTemplateAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.web.client.RestTemplate' (OnClassCondition)
      - NoneNestedConditions 0 matched 1 did not; NestedCondition on NotReactiveWebApplicationCondition.ReactiveWebApplication did not find reactive web application classes (NotReactiveWebApplicationCondition)

   RestTemplateAutoConfiguration#restTemplateBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.web.client.RestTemplateBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   ServletWebServerFactoryAutoConfiguration matched:
      - @ConditionalOnClass found required class 'jakarta.servlet.ServletRequest' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)

   ServletWebServerFactoryAutoConfiguration#tomcatServletWebServerFactoryCustomizer matched:
      - @ConditionalOnClass found required class 'org.apache.catalina.startup.Tomcat' (OnClassCondition)

   ServletWebServerFactoryConfiguration.EmbeddedTomcat matched:
      - @ConditionalOnClass found required classes 'jakarta.servlet.Servlet', 'org.apache.catalina.startup.Tomcat', 'org.apache.coyote.UpgradeProtocol' (OnClassCondition)
      - @ConditionalOnMissingBean (types: org.springframework.boot.web.servlet.server.ServletWebServerFactory; SearchStrategy: current) did not find any beans (OnBeanCondition)

   SimpleCacheConfiguration matched:
      - Cache org.springframework.boot.autoconfigure.cache.SimpleCacheConfiguration automatic cache type (CacheCondition)

   SpringDataWebAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'org.springframework.data.web.PageableHandlerMethodArgumentResolver', 'org.springframework.web.servlet.config.annotation.WebMvcConfigurer' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)
      - @ConditionalOnMissingBean (types: org.springframework.data.web.PageableHandlerMethodArgumentResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   SpringDataWebAutoConfiguration#pageableCustomizer matched:
      - @ConditionalOnMissingBean (types: org.springframework.data.web.config.PageableHandlerMethodArgumentResolverCustomizer; SearchStrategy: all) did not find any beans (OnBeanCondition)

   SpringDataWebAutoConfiguration#sortCustomizer matched:
      - @ConditionalOnMissingBean (types: org.springframework.data.web.config.SortHandlerMethodArgumentResolverCustomizer; SearchStrategy: all) did not find any beans (OnBeanCondition)

   SqlInitializationAutoConfiguration matched:
      - @ConditionalOnProperty (spring.sql.init.enabled) matched (OnPropertyCondition)
      - NoneNestedConditions 0 matched 1 did not; NestedCondition on SqlInitializationAutoConfiguration.SqlInitializationModeCondition.ModeIsNever @ConditionalOnProperty (spring.sql.init.mode=never) did not find property 'mode' (SqlInitializationAutoConfiguration.SqlInitializationModeCondition)

   SslAutoConfiguration#sslBundleRegistry matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.ssl.SslBundleRegistry,org.springframework.boot.ssl.SslBundles; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TaskExecutionAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor' (OnClassCondition)

   TaskExecutorConfigurations.SimpleAsyncTaskExecutorBuilderConfiguration#simpleAsyncTaskExecutorBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.SimpleAsyncTaskExecutorBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)
      - @ConditionalOnThreading found PLATFORM (OnThreadingCondition)

   TaskExecutorConfigurations.TaskExecutorBuilderConfiguration#taskExecutorBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.TaskExecutorBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TaskExecutorConfigurations.TaskExecutorConfiguration matched:
      - @ConditionalOnMissingBean (types: java.util.concurrent.Executor; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TaskExecutorConfigurations.TaskExecutorConfiguration#applicationTaskExecutor matched:
      - @ConditionalOnThreading found PLATFORM (OnThreadingCondition)

   TaskExecutorConfigurations.ThreadPoolTaskExecutorBuilderConfiguration#threadPoolTaskExecutorBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.TaskExecutorBuilder,org.springframework.boot.task.ThreadPoolTaskExecutorBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TaskSchedulingAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.scheduling.concurrent.ThreadPoolTaskScheduler' (OnClassCondition)

   TaskSchedulingConfigurations.SimpleAsyncTaskSchedulerBuilderConfiguration#simpleAsyncTaskSchedulerBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.SimpleAsyncTaskSchedulerBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)
      - @ConditionalOnThreading found PLATFORM (OnThreadingCondition)

   TaskSchedulingConfigurations.TaskSchedulerBuilderConfiguration#taskSchedulerBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.TaskSchedulerBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TaskSchedulingConfigurations.ThreadPoolTaskSchedulerBuilderConfiguration#threadPoolTaskSchedulerBuilder matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.task.TaskSchedulerBuilder,org.springframework.boot.task.ThreadPoolTaskSchedulerBuilder; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TransactionAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.transaction.PlatformTransactionManager' (OnClassCondition)

   TransactionAutoConfiguration.EnableTransactionManagementConfiguration matched:
      - @ConditionalOnBean (types: org.springframework.transaction.TransactionManager; SearchStrategy: all) found bean 'transactionManager'; @ConditionalOnMissingBean (types: org.springframework.transaction.annotation.AbstractTransactionManagementConfiguration; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TransactionAutoConfiguration.EnableTransactionManagementConfiguration.CglibAutoProxyConfiguration matched:
      - @ConditionalOnProperty (spring.aop.proxy-target-class=true) matched (OnPropertyCondition)

   TransactionAutoConfiguration.TransactionTemplateConfiguration matched:
      - @ConditionalOnSingleCandidate (types: org.springframework.transaction.PlatformTransactionManager; SearchStrategy: all) found a single bean 'transactionManager' (OnBeanCondition)

   TransactionAutoConfiguration.TransactionTemplateConfiguration#transactionTemplate matched:
      - @ConditionalOnMissingBean (types: org.springframework.transaction.support.TransactionOperations; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TransactionManagerCustomizationAutoConfiguration matched:
      - @ConditionalOnClass found required class 'org.springframework.transaction.PlatformTransactionManager' (OnClassCondition)

   TransactionManagerCustomizationAutoConfiguration#platformTransactionManagerCustomizers matched:
      - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.transaction.TransactionManagerCustomizers; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'jakarta.servlet.Servlet', 'org.springframework.web.servlet.DispatcherServlet', 'org.springframework.web.servlet.config.annotation.WebMvcConfigurer' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)
      - @ConditionalOnMissingBean (types: org.springframework.web.servlet.config.annotation.WebMvcConfigurationSupport; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration#formContentFilter matched:
      - @ConditionalOnProperty (spring.mvc.formcontent.filter.enabled) matched (OnPropertyCondition)
      - @ConditionalOnMissingBean (types: org.springframework.web.filter.FormContentFilter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.EnableWebMvcConfiguration#flashMapManager matched:
      - @ConditionalOnMissingBean (names: flashMapManager; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.EnableWebMvcConfiguration#localeResolver matched:
      - @ConditionalOnMissingBean (names: localeResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.EnableWebMvcConfiguration#themeResolver matched:
      - @ConditionalOnMissingBean (names: themeResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.WebMvcAutoConfigurationAdapter#defaultViewResolver matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.servlet.view.InternalResourceViewResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.WebMvcAutoConfigurationAdapter#requestContextFilter matched:
      - @ConditionalOnMissingBean (types: org.springframework.web.context.request.RequestContextListener,org.springframework.web.filter.RequestContextFilter; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebMvcAutoConfiguration.WebMvcAutoConfigurationAdapter#viewResolver matched:
      - @ConditionalOnBean (types: org.springframework.web.servlet.ViewResolver; SearchStrategy: all) found beans 'defaultViewResolver', 'beanNameViewResolver', 'mvcViewResolver'; @ConditionalOnMissingBean (names: viewResolver types: org.springframework.web.servlet.view.ContentNegotiatingViewResolver; SearchStrategy: all) did not find any beans (OnBeanCondition)

   WebSocketServletAutoConfiguration matched:
      - @ConditionalOnClass found required classes 'jakarta.servlet.Servlet', 'jakarta.websocket.server.ServerContainer' (OnClassCondition)
      - found 'session' scope (OnWebApplicationCondition)

   WebSocketServletAutoConfiguration.TomcatWebSocketConfiguration matched:
      - @ConditionalOnClass found required classes 'org.apache.catalina.startup.Tomcat', 'org.apache.tomcat.websocket.server.WsSci' (OnClassCondition)

   WebSocketServletAutoConfiguration.TomcatWebSocketConfiguration#websocketServletWebServerCustomizer matched:
      - @ConditionalOnMissingBean (names: websocketServletWebServerCustomizer; SearchStrategy: all) did not find any beans (OnBeanCondition)


Negative matches:
-----------------

   ActiveMQAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.jms.ConnectionFactory' (OnClassCondition)

   AopAutoConfiguration.AspectJAutoProxyingConfiguration.JdkDynamicAutoProxyConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.aop.proxy-target-class=false) did not find property 'proxy-target-class' (OnPropertyCondition)

   AopAutoConfiguration.ClassProxyingConfiguration:
      Did not match:
         - @ConditionalOnMissingClass found unwanted class 'org.aspectj.weaver.Advice' (OnClassCondition)

   ArtemisAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.jms.ConnectionFactory' (OnClassCondition)

   BatchAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.batch.core.launch.JobLauncher' (OnClassCondition)

   Cache2kCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.cache2k.Cache2kBuilder' (OnClassCondition)

   CacheAutoConfiguration:
      Did not match:
         - @ConditionalOnBean (types: org.springframework.cache.interceptor.CacheAspectSupport; SearchStrategy: all) did not find any beans of type org.springframework.cache.interceptor.CacheAspectSupport (OnBeanCondition)
      Matched:
         - @ConditionalOnClass found required class 'org.springframework.cache.CacheManager' (OnClassCondition)

   CacheAutoConfiguration.CacheManagerEntityManagerFactoryDependsOnPostProcessor:
      Did not match:
         - Ancestor org.springframework.boot.autoconfigure.cache.CacheAutoConfiguration did not match (ConditionEvaluationReport.AncestorsMatchedCondition)
      Matched:
         - @ConditionalOnClass found required class 'org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean' (OnClassCondition)

   CaffeineCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.github.benmanes.caffeine.cache.Caffeine' (OnClassCondition)

   CassandraAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.datastax.oss.driver.api.core.CqlSession' (OnClassCondition)

   CassandraDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.datastax.oss.driver.api.core.CqlSession' (OnClassCondition)

   CassandraReactiveDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.datastax.oss.driver.api.core.CqlSession' (OnClassCondition)

   CassandraReactiveRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.cassandra.ReactiveSession' (OnClassCondition)

   CassandraRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.datastax.oss.driver.api.core.CqlSession' (OnClassCondition)

   ClientHttpConnectorAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.function.client.WebClient' (OnClassCondition)

   CodecsAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.function.client.WebClient' (OnClassCondition)

   CouchbaseAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Cluster' (OnClassCondition)

   CouchbaseCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Cluster' (OnClassCondition)

   CouchbaseDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Bucket' (OnClassCondition)

   CouchbaseReactiveDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Cluster' (OnClassCondition)

   CouchbaseReactiveRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Cluster' (OnClassCondition)

   CouchbaseRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.couchbase.client.java.Bucket' (OnClassCondition)

   DataSourceAutoConfiguration.EmbeddedDatabaseConfiguration:
      Did not match:
         - EmbeddedDataSource spring.datasource.url is set (DataSourceAutoConfiguration.EmbeddedDatabaseCondition)

   DataSourceCheckpointRestoreConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.crac.Resource' (OnClassCondition)

   DataSourceConfiguration.Dbcp2:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.commons.dbcp2.BasicDataSource' (OnClassCondition)

   DataSourceConfiguration.Generic:
      Did not match:
         - @ConditionalOnProperty (spring.datasource.type) did not find property 'spring.datasource.type' (OnPropertyCondition)

   DataSourceConfiguration.OracleUcp:
      Did not match:
         - @ConditionalOnClass did not find required classes 'oracle.ucp.jdbc.PoolDataSourceImpl', 'oracle.jdbc.OracleConnection' (OnClassCondition)

   DataSourceConfiguration.Tomcat:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.tomcat.jdbc.pool.DataSource' (OnClassCondition)

   DataSourceJmxConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.jmx.enabled=true) found different value in property 'enabled' (OnPropertyCondition)

   DataSourcePoolMetadataProvidersConfiguration.CommonsDbcp2PoolDataSourceMetadataProviderConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.commons.dbcp2.BasicDataSource' (OnClassCondition)

   DataSourcePoolMetadataProvidersConfiguration.OracleUcpPoolDataSourceMetadataProviderConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required classes 'oracle.ucp.jdbc.PoolDataSource', 'oracle.jdbc.OracleConnection' (OnClassCondition)

   DataSourcePoolMetadataProvidersConfiguration.TomcatDataSourcePoolMetadataProviderConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.tomcat.jdbc.pool.DataSource' (OnClassCondition)

   DataSourceTransactionManagerAutoConfiguration.JdbcTransactionManagerConfiguration#transactionManager:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.transaction.TransactionManager; SearchStrategy: all) found beans of type 'org.springframework.transaction.TransactionManager' transactionManager (OnBeanCondition)

   DispatcherServletAutoConfiguration.DispatcherServletConfiguration#multipartResolver:
      Did not match:
         - @ConditionalOnBean (types: org.springframework.web.multipart.MultipartResolver; SearchStrategy: all) did not find any beans of type org.springframework.web.multipart.MultipartResolver (OnBeanCondition)

   ElasticsearchClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'co.elastic.clients.elasticsearch.ElasticsearchClient' (OnClassCondition)

   ElasticsearchDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.elasticsearch.client.elc.ElasticsearchTemplate' (OnClassCondition)

   ElasticsearchRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.elasticsearch.repository.ElasticsearchRepository' (OnClassCondition)

   ElasticsearchRestClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.elasticsearch.client.RestClientBuilder' (OnClassCondition)

   EmbeddedLdapAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.unboundid.ldap.listener.InMemoryDirectoryServer' (OnClassCondition)

   EmbeddedWebServerFactoryCustomizerAutoConfiguration:
      Did not match:
         - Application is deployed as a WAR file. (OnWarDeploymentCondition)
      Matched:
         - @ConditionalOnWebApplication (required) found 'session' scope (OnWebApplicationCondition)

   ErrorWebFluxAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.config.WebFluxConfigurer' (OnClassCondition)

   FlywayAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.flywaydb.core.Flyway' (OnClassCondition)

   FreeMarkerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'freemarker.template.Configuration' (OnClassCondition)

   GraphQlAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlQueryByExampleAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlQuerydslAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.querydsl.core.Query' (OnClassCondition)

   GraphQlRSocketAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlReactiveQueryByExampleAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlReactiveQuerydslAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.querydsl.core.Query' (OnClassCondition)

   GraphQlWebFluxAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlWebFluxSecurityAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlWebMvcAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GraphQlWebMvcSecurityAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   GroovyTemplateAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'groovy.text.markup.MarkupTemplateEngine' (OnClassCondition)

   GsonAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.google.gson.Gson' (OnClassCondition)

   GsonHttpMessageConvertersConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.google.gson.Gson' (OnClassCondition)

   H2ConsoleAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.h2.server.web.JakartaWebServlet' (OnClassCondition)

   HazelcastAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.hazelcast.core.HazelcastInstance' (OnClassCondition)

   HazelcastCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.hazelcast.core.HazelcastInstance' (OnClassCondition)

   HazelcastJpaDependencyAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.hazelcast.core.HazelcastInstance' (OnClassCondition)

   HttpHandlerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.DispatcherHandler' (OnClassCondition)

   HypermediaAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.hateoas.EntityModel' (OnClassCondition)

   InfinispanCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.infinispan.spring.embedded.provider.SpringEmbeddedCacheManager' (OnClassCondition)

   InfluxDbAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.influxdb.InfluxDB' (OnClassCondition)

   IntegrationAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.integration.config.EnableIntegration' (OnClassCondition)

   JCacheCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'javax.cache.Caching' (OnClassCondition)

   JacksonHttpMessageConvertersConfiguration.MappingJackson2XmlHttpMessageConverterConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.fasterxml.jackson.dataformat.xml.XmlMapper' (OnClassCondition)

   JdbcRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.jdbc.repository.config.AbstractJdbcConfiguration' (OnClassCondition)

   JerseyAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.glassfish.jersey.server.spring.SpringComponentProvider' (OnClassCondition)

   JmsAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.jms.Message' (OnClassCondition)

   JmxAutoConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.jmx.enabled=true) found different value in property 'enabled' (OnPropertyCondition)
      Matched:
         - @ConditionalOnClass found required class 'org.springframework.jmx.export.MBeanExporter' (OnClassCondition)

   JndiConnectionFactoryAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.jms.core.JmsTemplate' (OnClassCondition)

   JndiDataSourceAutoConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.datasource.jndi-name) did not find property 'jndi-name' (OnPropertyCondition)
      Matched:
         - @ConditionalOnClass found required classes 'javax.sql.DataSource', 'org.springframework.jdbc.datasource.embedded.EmbeddedDatabaseType' (OnClassCondition)

   JndiJtaConfiguration:
      Did not match:
         - @ConditionalOnJndi JNDI environment is not available (OnJndiCondition)
      Matched:
         - @ConditionalOnClass found required class 'org.springframework.transaction.jta.JtaTransactionManager' (OnClassCondition)

   JooqAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.jooq.DSLContext' (OnClassCondition)

   JpaRepositoriesAutoConfiguration#entityManagerFactoryBootstrapExecutorCustomizer:
      Did not match:
         - AnyNestedCondition 0 matched 2 did not; NestedCondition on JpaRepositoriesAutoConfiguration.BootstrapExecutorCondition.LazyBootstrapMode @ConditionalOnProperty (spring.data.jpa.repositories.bootstrap-mode=lazy) did not find property 'bootstrap-mode'; NestedCondition on JpaRepositoriesAutoConfiguration.BootstrapExecutorCondition.DeferredBootstrapMode @ConditionalOnProperty (spring.data.jpa.repositories.bootstrap-mode=deferred) did not find property 'bootstrap-mode' (JpaRepositoriesAutoConfiguration.BootstrapExecutorCondition)

   JsonbAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.json.bind.Jsonb' (OnClassCondition)

   JsonbHttpMessageConvertersConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.json.bind.Jsonb' (OnClassCondition)

   KafkaAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.kafka.core.KafkaTemplate' (OnClassCondition)

   LdapAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.ldap.core.ContextSource' (OnClassCondition)

   LdapRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.ldap.repository.LdapRepository' (OnClassCondition)

   LiquibaseAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'liquibase.change.DatabaseChange' (OnClassCondition)

   MailSenderAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.mail.internet.MimeMessage' (OnClassCondition)

   MailSenderValidatorAutoConfiguration:
      Did not match:
         - @ConditionalOnSingleCandidate did not find required type 'org.springframework.mail.javamail.JavaMailSenderImpl' (OnBeanCondition)

   MessageSourceAutoConfiguration:
      Did not match:
         - ResourceBundle did not find bundle with basename messages (MessageSourceAutoConfiguration.ResourceBundleCondition)

   MockMvcAutoConfiguration#dispatcherServlet:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.web.servlet.DispatcherServlet; SearchStrategy: all) found beans of type 'org.springframework.web.servlet.DispatcherServlet' dispatcherServlet (OnBeanCondition)

   MockMvcAutoConfiguration#dispatcherServletPath:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.boot.autoconfigure.web.servlet.DispatcherServletPath; SearchStrategy: all) found beans of type 'org.springframework.boot.autoconfigure.web.servlet.DispatcherServletPath' dispatcherServletRegistration (OnBeanCondition)

   MockMvcAutoConfiguration.WebTestClientMockMvcConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.function.client.WebClient' (OnClassCondition)

   MockMvcSecurityConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.test.web.servlet.request.SecurityMockMvcRequestPostProcessors' (OnClassCondition)

   MockMvcWebClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.gargoylesoftware.htmlunit.WebClient' (OnClassCondition)

   MockMvcWebDriverAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.openqa.selenium.htmlunit.HtmlUnitDriver' (OnClassCondition)

   MongoAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.client.MongoClient' (OnClassCondition)

   MongoDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.client.MongoClient' (OnClassCondition)

   MongoReactiveAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.reactivestreams.client.MongoClient' (OnClassCondition)

   MongoReactiveDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.reactivestreams.client.MongoClient' (OnClassCondition)

   MongoReactiveRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.reactivestreams.client.MongoClient' (OnClassCondition)

   MongoRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.mongodb.client.MongoClient' (OnClassCondition)

   MustacheAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.samskivert.mustache.Mustache' (OnClassCondition)

   Neo4jAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.neo4j.driver.Driver' (OnClassCondition)

   Neo4jDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.neo4j.driver.Driver' (OnClassCondition)

   Neo4jReactiveDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.neo4j.driver.Driver' (OnClassCondition)

   Neo4jReactiveRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.neo4j.driver.Driver' (OnClassCondition)

   Neo4jRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.neo4j.driver.Driver' (OnClassCondition)

   NettyAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.netty.util.NettyRuntime' (OnClassCondition)

   OAuth2AuthorizationServerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.oauth2.server.authorization.OAuth2Authorization' (OnClassCondition)

   OAuth2AuthorizationServerJwtAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.oauth2.server.authorization.OAuth2Authorization' (OnClassCondition)

   OAuth2ClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.config.annotation.web.configuration.EnableWebSecurity' (OnClassCondition)

   OAuth2ResourceServerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.oauth2.server.resource.authentication.BearerTokenAuthenticationToken' (OnClassCondition)

   ProjectInfoAutoConfiguration#buildProperties:
      Did not match:
         - @ConditionalOnResource did not find resource '${spring.info.build.location:classpath:META-INF/build-info.properties}' (OnResourceCondition)

   ProjectInfoAutoConfiguration#gitProperties:
      Did not match:
         - GitResource did not find git info at classpath:git.properties (ProjectInfoAutoConfiguration.GitResourceAvailableCondition)

   PulsarAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.pulsar.client.api.PulsarClient' (OnClassCondition)

   PulsarReactiveAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.apache.pulsar.client.api.PulsarClient' (OnClassCondition)

   QuartzAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.quartz.Scheduler' (OnClassCondition)

   R2dbcAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.r2dbc.spi.ConnectionFactory' (OnClassCondition)

   R2dbcDataAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.r2dbc.core.R2dbcEntityTemplate' (OnClassCondition)

   R2dbcInitializationConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required classes 'io.r2dbc.spi.ConnectionFactory', 'org.springframework.r2dbc.connection.init.DatabasePopulator' (OnClassCondition)

   R2dbcRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.r2dbc.spi.ConnectionFactory' (OnClassCondition)

   R2dbcTransactionManagerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.r2dbc.connection.R2dbcTransactionManager' (OnClassCondition)

   RSocketGraphQlClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'graphql.GraphQL' (OnClassCondition)

   RSocketMessagingAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.rsocket.RSocket' (OnClassCondition)

   RSocketRequesterAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.rsocket.RSocket' (OnClassCondition)

   RSocketSecurityAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.rsocket.core.SecuritySocketAcceptorInterceptor' (OnClassCondition)

   RSocketServerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.rsocket.core.RSocketServer' (OnClassCondition)

   RSocketStrategiesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.netty.buffer.PooledByteBufAllocator' (OnClassCondition)

   RabbitAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.rabbitmq.client.Channel' (OnClassCondition)

   ReactiveElasticsearchClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'co.elastic.clients.transport.ElasticsearchTransport' (OnClassCondition)

   ReactiveElasticsearchRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.elasticsearch.client.elc.ReactiveElasticsearchClient' (OnClassCondition)

   ReactiveMultipartAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.config.WebFluxConfigurer' (OnClassCondition)

   ReactiveOAuth2ClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'reactor.core.publisher.Flux' (OnClassCondition)

   ReactiveOAuth2ResourceServerAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.config.annotation.web.reactive.EnableWebFluxSecurity' (OnClassCondition)

   ReactiveSecurityAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'reactor.core.publisher.Flux' (OnClassCondition)

   ReactiveUserDetailsServiceAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.authentication.ReactiveAuthenticationManager' (OnClassCondition)

   ReactiveWebServerFactoryAutoConfiguration:
      Did not match:
         - @ConditionalOnWebApplication did not find reactive web application classes (OnWebApplicationCondition)

   ReactorAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'reactor.core.publisher.Hooks' (OnClassCondition)

   RedisAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.redis.core.RedisOperations' (OnClassCondition)

   RedisCacheConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.redis.connection.RedisConnectionFactory' (OnClassCondition)

   RedisReactiveAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'reactor.core.publisher.Flux' (OnClassCondition)

   RedisRepositoriesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.redis.repository.configuration.EnableRedisRepositories' (OnClassCondition)

   RepositoryRestMvcAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.data.rest.webmvc.config.RepositoryRestMvcConfiguration' (OnClassCondition)

   Saml2RelyingPartyAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.saml2.provider.service.registration.RelyingPartyRegistrationRepository' (OnClassCondition)

   SecurityAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.authentication.DefaultAuthenticationEventPublisher' (OnClassCondition)

   SecurityFilterAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.config.http.SessionCreationPolicy' (OnClassCondition)

   SendGridAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'com.sendgrid.SendGrid' (OnClassCondition)

   ServletWebServerFactoryAutoConfiguration.ForwardedHeaderFilterConfiguration:
      Did not match:
         - @ConditionalOnProperty (server.forward-headers-strategy=framework) did not find property 'server.forward-headers-strategy' (OnPropertyCondition)

   ServletWebServerFactoryConfiguration.EmbeddedJetty:
      Did not match:
         - @ConditionalOnClass did not find required classes 'org.eclipse.jetty.server.Server', 'org.eclipse.jetty.util.Loader', 'org.eclipse.jetty.ee10.webapp.WebAppContext' (OnClassCondition)

   ServletWebServerFactoryConfiguration.EmbeddedUndertow:
      Did not match:
         - @ConditionalOnClass did not find required classes 'io.undertow.Undertow', 'org.xnio.SslClientAuthMode' (OnClassCondition)

   SessionAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.session.Session' (OnClassCondition)

   SpringApplicationAdminJmxAutoConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.application.admin.enabled=true) did not find property 'enabled' (OnPropertyCondition)

   TaskExecutorConfigurations.SimpleAsyncTaskExecutorBuilderConfiguration#simpleAsyncTaskExecutorBuilderVirtualThreads:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.boot.task.SimpleAsyncTaskExecutorBuilder; SearchStrategy: all) found beans of type 'org.springframework.boot.task.SimpleAsyncTaskExecutorBuilder' simpleAsyncTaskExecutorBuilder (OnBeanCondition)

   TaskExecutorConfigurations.TaskExecutorConfiguration#applicationTaskExecutorVirtualThreads:
      Did not match:
         - @ConditionalOnThreading did not find VIRTUAL (OnThreadingCondition)

   TaskSchedulingAutoConfiguration#scheduledBeanLazyInitializationExcludeFilter:
      Did not match:
         - @ConditionalOnBean (names: org.springframework.context.annotation.internalScheduledAnnotationProcessor; SearchStrategy: all) did not find any beans named org.springframework.context.annotation.internalScheduledAnnotationProcessor (OnBeanCondition)

   TaskSchedulingConfigurations.SimpleAsyncTaskSchedulerBuilderConfiguration#simpleAsyncTaskSchedulerBuilderVirtualThreads:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.boot.task.SimpleAsyncTaskSchedulerBuilder; SearchStrategy: all) found beans of type 'org.springframework.boot.task.SimpleAsyncTaskSchedulerBuilder' simpleAsyncTaskSchedulerBuilder (OnBeanCondition)

   TaskSchedulingConfigurations.TaskSchedulerConfiguration:
      Did not match:
         - @ConditionalOnBean (names: org.springframework.context.annotation.internalScheduledAnnotationProcessor; SearchStrategy: all) did not find any beans named org.springframework.context.annotation.internalScheduledAnnotationProcessor (OnBeanCondition)

   ThymeleafAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.thymeleaf.spring6.SpringTemplateEngine' (OnClassCondition)

   TransactionAutoConfiguration#transactionalOperator:
      Did not match:
         - @ConditionalOnSingleCandidate (types: org.springframework.transaction.ReactiveTransactionManager; SearchStrategy: all) did not find any beans (OnBeanCondition)

   TransactionAutoConfiguration.AspectJTransactionManagementConfiguration:
      Did not match:
         - @ConditionalOnBean (types: org.springframework.transaction.aspectj.AbstractTransactionAspect; SearchStrategy: all) did not find any beans of type org.springframework.transaction.aspectj.AbstractTransactionAspect (OnBeanCondition)

   TransactionAutoConfiguration.EnableTransactionManagementConfiguration.JdkDynamicAutoProxyConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.aop.proxy-target-class=false) did not find property 'proxy-target-class' (OnPropertyCondition)

   UserDetailsServiceAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.security.authentication.AuthenticationManager' (OnClassCondition)

   ValidationAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'jakarta.validation.executable.ExecutableValidator' (OnClassCondition)

   WebClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.function.client.WebClient' (OnClassCondition)

   WebFluxAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.config.WebFluxConfigurer' (OnClassCondition)

   WebMvcAutoConfiguration#hiddenHttpMethodFilter:
      Did not match:
         - @ConditionalOnProperty (spring.mvc.hiddenmethod.filter.enabled) did not find property 'enabled' (OnPropertyCondition)

   WebMvcAutoConfiguration.ProblemDetailsErrorHandlingConfiguration:
      Did not match:
         - @ConditionalOnProperty (spring.mvc.problemdetails.enabled=true) did not find property 'enabled' (OnPropertyCondition)

   WebMvcAutoConfiguration.ResourceChainCustomizerConfiguration:
      Did not match:
         - @ConditionalOnEnabledResourceChain did not find class org.webjars.WebJarAssetLocator (OnEnabledResourceChainCondition)

   WebMvcAutoConfiguration.WebMvcAutoConfigurationAdapter#beanNameViewResolver:
      Did not match:
         - @ConditionalOnMissingBean (types: org.springframework.web.servlet.view.BeanNameViewResolver; SearchStrategy: all) found beans of type 'org.springframework.web.servlet.view.BeanNameViewResolver' beanNameViewResolver (OnBeanCondition)

   WebServiceTemplateAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.oxm.Marshaller' (OnClassCondition)

   WebServicesAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.ws.transport.http.MessageDispatcherServlet' (OnClassCondition)

   WebSessionIdResolverAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'reactor.core.publisher.Mono' (OnClassCondition)

   WebSocketMessagingAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.socket.config.annotation.WebSocketMessageBrokerConfigurer' (OnClassCondition)

   WebSocketReactiveAutoConfiguration:
      Did not match:
         - @ConditionalOnWebApplication did not find reactive web application classes (OnWebApplicationCondition)

   WebSocketServletAutoConfiguration.JettyWebSocketConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.eclipse.jetty.ee10.websocket.jakarta.server.config.JakartaWebSocketServletContainerInitializer' (OnClassCondition)

   WebSocketServletAutoConfiguration.UndertowWebSocketConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'io.undertow.websockets.jsr.Bootstrap' (OnClassCondition)

   WebTestClientAutoConfiguration:
      Did not match:
         - @ConditionalOnClass did not find required class 'org.springframework.web.reactive.function.client.WebClient' (OnClassCondition)

   XADataSourceAutoConfiguration:
      Did not match:
         - @ConditionalOnBean (types: org.springframework.boot.jdbc.XADataSourceWrapper; SearchStrategy: all) did not find any beans of type org.springframework.boot.jdbc.XADataSourceWrapper (OnBeanCondition)
      Matched:
         - @ConditionalOnClass found required classes 'javax.sql.DataSource', 'jakarta.transaction.TransactionManager', 'org.springframework.jdbc.datasource.embedded.EmbeddedDatabaseType' (OnClassCondition)


Exclusions:
-----------

    None


Unconditional classes:
----------------------

    org.springframework.boot.autoconfigure.context.ConfigurationPropertiesAutoConfiguration

    org.springframework.boot.autoconfigure.ssl.SslAutoConfiguration

    org.springframework.boot.autoconfigure.context.LifecycleAutoConfiguration

    org.springframework.boot.autoconfigure.context.PropertyPlaceholderAutoConfiguration

    org.springframework.boot.autoconfigure.availability.ApplicationAvailabilityAutoConfiguration

    org.springframework.boot.autoconfigure.info.ProjectInfoAutoConfiguration



2026-03-27T17:02:38.157+08:00  INFO 64007 --- [           main] clawer.api.RankingApiIntegrationTest     : Started RankingApiIntegrationTest in 4.43 seconds (process running for 5.54)
2026-03-27T17:02:38.159+08:00 DEBUG 64007 --- [           main] o.s.b.a.ApplicationAvailabilityBean      : Application availability state LivenessState changed to CORRECT
2026-03-27T17:02:38.162+08:00 DEBUG 64007 --- [           main] o.s.b.a.ApplicationAvailabilityBean      : Application availability state ReadinessState changed to ACCEPTING_TRAFFIC
2026-03-27T17:02:38.846+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:38.865+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:38.865+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:38.869+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:38.869+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:38.885+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:38.888+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:38.889+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:38.932+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:38.933+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@495598c5]
2026-03-27T17:02:38.954+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:38.955+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:38.956+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:38.956+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:38.956+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:38.959+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=4, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:38.959+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:38.959+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:38.965+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:38.966+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@4615bc00]
]]></system-out>
    <system-err><![CDATA[WARNING: A Java agent has been loaded dynamically (/Users/test/.m2/repository/net/bytebuddy/byte-buddy-agent/1.14.12/byte-buddy-agent-1.14.12.jar)
WARNING: If a serviceability tool is in use, please run with -XX:+EnableDynamicAgentLoading to hide this warning
WARNING: If a serviceability tool is not in use, please run with -Djdk.instrument.traceUsage for more information
WARNING: Dynamic loading of agents will be disallowed by default in a future release
]]></system-err>
  </testcase>
  <testcase name="pageSizeUsesStrictRowCountEvenWhenRanksContainTies" classname="clawer.api.RankingApiIntegrationTest" time="0.077">
    <system-out><![CDATA[2026-03-27T17:02:39.035+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.036+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=25, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.036+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=25, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.036+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.036+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.040+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=25, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.040+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.040+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.050+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.050+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@6c4c32ff]
2026-03-27T17:02:39.053+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.054+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=2, pageSize=25, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.054+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=2, pageSize=25, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.055+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.055+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.058+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=2, pageSize=25, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.058+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.058+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.065+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.065+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@5a1a5521]
]]></system-out>
  </testcase>
  <testcase name="searchForEthReturnsEthZurichAndAvoidsFalsePositives" classname="clawer.api.RankingApiIntegrationTest" time="0.073">
    <system-out><![CDATA[2026-03-27T17:02:39.112+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.113+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=ETH, scope=null, region=null
2026-03-27T17:02:39.113+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=ETH, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.114+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.114+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.119+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=1, page=1, pageSize=20, source=AGGREGATED, year=2099, search=ETH, scope=global, region=null
2026-03-27T17:02:39.120+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.120+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.122+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.122+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.125+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='ETH', scope='global', region='null', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=29, rowsAfterSearch=1
2026-03-27T17:02:39.126+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.126+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.136+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.137+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@adbc3bc]
]]></system-out>
  </testcase>
  <testcase name="latestUniverseViewAndRegionReadPathDoNotExplodeRowsWhenOlderMethodsExist" classname="clawer.api.RankingApiIntegrationTest" time="0.069">
    <system-out><![CDATA[2026-03-27T17:02:39.184+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL update
2026-03-27T17:02:39.185+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [INSERT INTO analytics.aggregation_runs (
    aggregation_run_id,
    run_label,
    ranking_year,
    universe_type,
    universe_key,
    aggregation_method_version,
    status,
    input_record_count,
    output_record_count
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
]
2026-03-27T17:02:39.187+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL update
2026-03-27T17:02:39.187+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [INSERT INTO analytics.aggregated_rankings (
    aggregation_run_id,
    canonical_university_id,
    ranking_year,
    universe_type,
    universe_key,
    display_rank,
    composite_score,
    coverage_ratio,
    source_ranks_json,
    source_normalized_scores_json,
    source_weights_used_json,
    aggregation_method_version
)
SELECT
    ?,
    canonical_university_id,
    ranking_year,
    universe_type,
    universe_key,
    display_rank,
    composite_score,
    coverage_ratio,
    source_ranks_json,
    source_normalized_scores_json,
    source_weights_used_json,
    ?
FROM analytics.aggregated_rankings
WHERE aggregation_run_id = ?
  AND universe_type = 'global'
  AND universe_key = 'global'
]
2026-03-27T17:02:39.190+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing SQL query [SELECT COUNT(*)
FROM analytics.v_aggregated_rankings_latest
WHERE ranking_year = 2099
  AND universe_type = 'region'
  AND universe_key = 'europe'
]
2026-03-27T17:02:39.192+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing SQL query [SELECT COUNT(DISTINCT canonical_university_id)
FROM analytics.v_aggregated_rankings_latest
WHERE ranking_year = 2099
  AND universe_type = 'region'
  AND universe_key = 'europe'
]
2026-03-27T17:02:39.195+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.195+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.195+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:39.196+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.196+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.198+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=4, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.198+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.198+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.204+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.205+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@1ea78c72]
]]></system-out>
  </testcase>
  <testcase name="regionAggregationRanksAreContiguousWithoutNulls" classname="clawer.api.RankingApiIntegrationTest" time="0.072">
    <system-out><![CDATA[2026-03-27T17:02:39.261+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.261+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.262+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:39.262+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.262+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.265+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=4, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.266+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.266+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.271+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.272+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@3437fc4f]
]]></system-out>
  </testcase>
  <testcase name="countrySearchReturnsUnitedKingdomUniversities" classname="clawer.api.RankingApiIntegrationTest" time="0.092">
    <system-out><![CDATA[2026-03-27T17:02:39.366+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.367+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=United Kingdom, scope=null, region=null
2026-03-27T17:02:39.367+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=United Kingdom, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.368+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.368+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.373+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=1, page=1, pageSize=20, source=AGGREGATED, year=2099, search=United Kingdom, scope=global, region=null
2026-03-27T17:02:39.373+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.373+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.375+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.376+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.378+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='United Kingdom', scope='global', region='null', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=29, rowsAfterSearch=1
2026-03-27T17:02:39.379+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.379+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.384+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.384+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@709c6768]
]]></system-out>
  </testcase>
  <testcase name="pageTwoContinuesAfterPageOneWithoutRepeats" classname="clawer.api.RankingApiIntegrationTest" time="0.068">
    <system-out><![CDATA[2026-03-27T17:02:39.425+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.426+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.426+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.426+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.426+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.430+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.430+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.430+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.437+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.438+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@3e356ed6]
2026-03-27T17:02:39.441+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.441+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=2, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.442+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=2, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.442+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.442+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.444+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=2, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.444+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.444+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.452+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.452+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@403b7be3]
]]></system-out>
  </testcase>
  <testcase name="countrySearchMatchesCountryWithoutOverridingNamePriority" classname="clawer.api.RankingApiIntegrationTest" time="0.051">
    <system-out><![CDATA[2026-03-27T17:02:39.491+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.492+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=Netherlands, scope=null, region=null
2026-03-27T17:02:39.493+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=Netherlands, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.493+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.493+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.496+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=1, page=1, pageSize=20, source=AGGREGATED, year=2099, search=Netherlands, scope=global, region=null
2026-03-27T17:02:39.497+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.497+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.499+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.499+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.501+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='Netherlands', scope='global', region='null', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=29, rowsAfterSearch=1
2026-03-27T17:02:39.501+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.501+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.507+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.507+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@2a5d9ede]
]]></system-out>
  </testcase>
  <testcase name="pageOneIsGloballySortedAndDeterministic" classname="clawer.api.RankingApiIntegrationTest" time="0.059">
    <system-out><![CDATA[2026-03-27T17:02:39.543+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.544+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.544+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.544+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.544+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.547+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.548+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.548+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.554+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.554+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@39e91bda]
2026-03-27T17:02:39.556+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.557+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.557+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.557+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.557+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.559+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.560+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.560+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.566+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.566+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@2c3f81ef]
]]></system-out>
  </testcase>
  <testcase name="regionScopeReRanksWithinRegionAndPreservesGlobalRank" classname="clawer.api.RankingApiIntegrationTest" time="0.042">
    <system-out><![CDATA[2026-03-27T17:02:39.603+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.604+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.604+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:39.604+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.604+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.607+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=4, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=region, region=Europe
2026-03-27T17:02:39.607+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.607+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.611+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.612+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@430dbcb0]
]]></system-out>
  </testcase>
  <testcase name="aggregatedRankingsEndpointReturnsSuccess" classname="clawer.api.RankingApiIntegrationTest" time="0.065">
    <system-out><![CDATA[2026-03-27T17:02:39.643+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.644+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.644+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.644+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.644+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.647+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.647+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.647+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.654+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.654+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@726d0482]
]]></system-out>
  </testcase>
  <testcase name="emptyResultForMissingYearKeepsResponseShapeAndDoesNotFallback" classname="clawer.api.RankingApiIntegrationTest" time="0.037">
    <system-out><![CDATA[2026-03-27T17:02:39.709+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.710+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2098, search=null, scope=null, region=null
2026-03-27T17:02:39.710+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2098, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.710+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.710+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.712+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=0, page=1, pageSize=20, source=AGGREGATED, year=2098, search=null, scope=global, region=null
2026-03-27T17:02:39.713+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.713+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.715+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.716+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@22d23c27]
]]></system-out>
  </testcase>
  <testcase name="compatibilityRouteOnlyWorksForAggregated" classname="clawer.api.RankingApiIntegrationTest" time="0.048">
    <system-out><![CDATA[2026-03-27T17:02:39.746+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getRankingsBySource(String, int, Integer, Integer, String, String, String)
2026-03-27T17:02:39.751+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings/AGGREGATED called with page=1, pageSize=20, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.751+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=null, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.751+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.751+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.754+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=29, page=1, pageSize=20, source=AGGREGATED, year=2099, search=null, scope=global, region=null
2026-03-27T17:02:39.754+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.754+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.761+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.762+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@285b63eb]
2026-03-27T17:02:39.764+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getRankingsBySource(String, int, Integer, Integer, String, String, String)
2026-03-27T17:02:39.765+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings/QS called with page=1, pageSize=20, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:39.765+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.765+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@5a983f8a]
]]></system-out>
  </testcase>
  <testcase name="longerSearchCanStillReturnRelevantTechnologyMatches" classname="clawer.api.RankingApiIntegrationTest" time="0.063">
    <system-out><![CDATA[2026-03-27T17:02:39.806+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.807+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=technology, scope=null, region=null
2026-03-27T17:02:39.807+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=technology, scope=global, region=null, rankingLabel=Global Rankings
2026-03-27T17:02:39.807+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.807+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.811+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=2, page=1, pageSize=20, source=AGGREGATED, year=2099, search=technology, scope=global, region=null
2026-03-27T17:02:39.811+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.811+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.813+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.813+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.815+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='technology', scope='global', region='null', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=29, rowsAfterSearch=2
2026-03-27T17:02:39.815+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.815+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.820+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.829+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@3c89fdd8]
]]></system-out>
  </testcase>
  <testcase name="regionScopeSearchRespectsRegionUniverseBeforePagination" classname="clawer.api.RankingApiIntegrationTest" time="0.049">
    <system-out><![CDATA[2026-03-27T17:02:39.866+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.867+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=Netherlands, scope=region, region=Europe
2026-03-27T17:02:39.867+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=Netherlands, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:39.867+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.867+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.870+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=1, page=1, pageSize=20, source=AGGREGATED, year=2099, search=Netherlands, scope=region, region=Europe
2026-03-27T17:02:39.870+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.870+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.872+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.872+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.874+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='Netherlands', scope='region', region='Europe', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=4, rowsAfterSearch=1
2026-03-27T17:02:39.874+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.874+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.879+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.879+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@2e895acb]
]]></system-out>
  </testcase>
  <testcase name="regionScopeRequiresValidRegion" classname="clawer.api.RankingApiIntegrationTest" time="0.037">
    <system-out><![CDATA[2026-03-27T17:02:39.914+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.916+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=null, year=2099, search=null, scope=region, region=null
2026-03-27T17:02:39.916+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.916+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@5d1d05ff]
2026-03-27T17:02:39.918+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.919+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=null, year=2099, search=null, scope=region, region=Atlantis
2026-03-27T17:02:39.919+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.919+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@5889e697]
]]></system-out>
  </testcase>
  <testcase name="regionScopeCountrySearchReturnsUnitedKingdomUniversities" classname="clawer.api.RankingApiIntegrationTest" time="0.049">
    <system-out><![CDATA[2026-03-27T17:02:39.955+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:39.955+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=AGGREGATED, year=2099, search=United Kingdom, scope=region, region=Europe
2026-03-27T17:02:39.955+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings request received: page=1, pageSize=20, year=2099, search=United Kingdom, scope=region, region=Europe, rankingLabel=Europe Rankings
2026-03-27T17:02:39.956+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.956+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.959+08:00  INFO 64007 --- [           main] clawer.service.RankingService            : Aggregated rankings counts: totalAfterFiltering=1, page=1, pageSize=20, source=AGGREGATED, year=2099, search=United Kingdom, scope=region, region=Europe
2026-03-27T17:02:39.960+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.960+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.962+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.962+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT COUNT(*)
FROM ranked
WHERE (? = '' OR search_score > 0)
]
2026-03-27T17:02:39.964+08:00  INFO 64007 --- [           main] c.r.JdbcScopedRankingReadAdapter         : Scoped ranking search debug: search='United Kingdom', scope='region', region='Europe', whereClause='LOWER(university_name) LIKE %search% OR LOWER(country_name) LIKE %search% OR alias match', rowsBeforeSearch=4, rowsAfterSearch=1
2026-03-27T17:02:39.964+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL query
2026-03-27T17:02:39.964+08:00 DEBUG 64007 --- [           main] o.s.jdbc.core.JdbcTemplate               : Executing prepared SQL statement [WITH admission_summary AS (
    SELECT
        cul.canonical_university_id,
        MIN(ar.ielts_min) FILTER (WHERE ar.ielts_min IS NOT NULL) AS ielts_min
    FROM warehouse.canonical_university_link cul
    JOIN warehouse.admission_requirements ar
      ON ar.university_id = cul.university_id
    GROUP BY cul.canonical_university_id
),
global_reference AS (
    SELECT
        canonical_university_id,
        MIN(display_rank) AS global_rank
    FROM analytics.v_aggregated_rankings_latest
    WHERE (? IS NULL OR ranking_year = ?)
      AND universe_type = 'global'
      AND universe_key = 'global'
    GROUP BY canonical_university_id
),
scoped_base AS (
    SELECT
        ar.canonical_university_id,
        cu.display_name AS university_name,
        cu.canonical_slug AS slug,
        c.country_name AS country,
        ar.ranking_year,
        COALESCE(global_ref.global_rank, ar.display_rank) AS global_rank,
        ar.display_rank AS scope_rank,
        ar.composite_score,
        ar.coverage_ratio,
        ads.ielts_min,
        ar.source_ranks_json,
        ar.aggregation_method_version,
        COALESCE(c.region_name, '') AS region_name,
        COALESCE((
            SELECT COUNT(*)
            FROM jsonb_object_keys(ar.source_ranks_json)
        ), 0) AS source_count,
        GREATEST(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(cu.display_name) = lower(?)
                  OR cu.display_name_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(cu.display_name) LIKE lower(?) || '%'
                  OR cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN cu.display_name_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR cu.display_name_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(cu.display_name) LIKE '%' || lower(?) || '%'
                      OR cu.display_name_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND lower(COALESCE(c.country_name, '')) LIKE '%' || lower(?) || '%'
                THEN 20
                ELSE 0
            END,
            COALESCE(alias_match.alias_search_score, 0)
        ) AS search_score
    FROM analytics.v_aggregated_rankings_latest ar
    JOIN warehouse.canonical_university cu
      ON cu.canonical_university_id = ar.canonical_university_id
    LEFT JOIN warehouse.countries c
      ON c.country_id = cu.country_id
    LEFT JOIN LATERAL (
        SELECT MAX(
            CASE
                WHEN ? = '' THEN 0
                WHEN lower(ua.alias_text) = lower(?)
                  OR ua.alias_normalized = regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 100
                WHEN lower(ua.alias_text) LIKE lower(?) || '%'
                  OR ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                THEN 80
                WHEN ua.alias_normalized LIKE regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || ' %'
                  OR ua.alias_normalized LIKE '% ' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')
                THEN 60
                WHEN char_length(regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g')) >= 4
                  AND (
                      lower(ua.alias_text) LIKE '%' || lower(?) || '%'
                      OR ua.alias_normalized LIKE '%' || regexp_replace(lower(?), '[^a-z0-9]+', ' ', 'g') || '%'
                  )
                THEN 40
                ELSE 0
            END
        ) AS alias_search_score
        FROM warehouse.university_alias ua
        WHERE ua.canonical_university_id = cu.canonical_university_id
    ) alias_match ON TRUE
    LEFT JOIN global_reference global_ref
      ON global_ref.canonical_university_id = ar.canonical_university_id
    LEFT JOIN admission_summary ads
      ON ads.canonical_university_id = ar.canonical_university_id
    WHERE (? IS NULL OR ar.ranking_year = ?)
      AND ar.universe_type = ?
      AND ar.universe_key = ?
),
ranked AS (
    SELECT
        canonical_university_id,
        university_name,
        slug,
        country,
        ranking_year,
        global_rank,
        scope_rank,
        composite_score,
        coverage_ratio,
        ielts_min,
        source_ranks_json,
        aggregation_method_version,
        source_count,
        search_score
    FROM scoped_base
)
SELECT
    canonical_university_id,
    university_name,
    slug,
    country,
    ranking_year,
    global_rank,
    scope_rank,
    composite_score,
    coverage_ratio,
    ielts_min,
    source_ranks_json,
    aggregation_method_version,
    source_count
FROM ranked
WHERE 1=1
 AND (? = '' OR search_score > 0)ORDER BY
    CASE WHEN ? THEN 0 ELSE search_score END DESC,
    CASE WHEN scope_rank IS NULL THEN 1 ELSE 0 END,
    scope_rank ASC,
    university_name ASC,
    canonical_university_id ASC
 LIMIT ? OFFSET ?]
2026-03-27T17:02:39.969+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:39.969+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@2c3d1214]
]]></system-out>
  </testcase>
  <testcase name="unsupportedSourceReturnsBadRequest" classname="clawer.api.RankingApiIntegrationTest" time="0.044">
    <system-out><![CDATA[2026-03-27T17:02:40.007+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:40.008+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=QS, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:40.008+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:40.008+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@48064a5a]
2026-03-27T17:02:40.010+08:00 DEBUG 64007 --- [           main] s.w.s.m.m.a.RequestMappingHandlerMapping : Mapped to clawer.api.RankingController#getAllRankings(int, Integer, String, Integer, String, String, String)
2026-03-27T17:02:40.011+08:00  INFO 64007 --- [           main] clawer.api.RankingController             : GET /api/v1/rankings called with page=1, pageSize=20, source=THE, year=2099, search=null, scope=null, region=null
2026-03-27T17:02:40.011+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Using 'application/json', given [application/json] and supported [application/json, application/*+json]
2026-03-27T17:02:40.011+08:00 DEBUG 64007 --- [           main] o.s.w.s.m.m.a.HttpEntityMethodProcessor  : Writing [clawer.dto.ApiResponse@7d018c58]
]]></system-out>
  </testcase>
</testsuite>